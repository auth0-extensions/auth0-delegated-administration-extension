// Stub for the @a0/auth0-extension-ui barrel dep the app never uses
// (DragAndDrop -> react-dropzone). The barrel statically imports it, but that
// component is unused here, so its bindings are never invoked at runtime.
module.exports = {};
